Next.js app for the job-insights website. See `AGENTS.md` for project
conventions and `EXECUTION_PLAN.md` for the broader plan.

## Run

```bash
pnpm install
pnpm dev      # http://localhost:3000
pnpm build    # static export to ./out
```
